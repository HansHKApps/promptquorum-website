node_modules/
out/
.next/
/app/
.env
.env.local
public/data/seo-registry.json
tsconfig.tsbuildinfo

# macOS Finder metadata
.DS_Store

# Playwright run artifacts (regenerated on every run)
test-results/
playwright-report/

# Office / Word lock files anywhere in the tree
~$*

# Backup files from sed -i.bak operations
*.bak

# GEO audit output (generated; re-run scripts/geo/geo-audit-scan.mjs to refresh)
geo-audit-report.csv
*.audit.csv

# Screenshot / image artifacts dropped in the tree (e.g. Playwright captures)
*.png
# ...except real content assets (Discover-compliant hero PNGs, brand images)
!public/images/*.png
!public/*.png

# Claude Code working directory (agent worktrees, session state)
.claude/

# Freshness tool: manual GSC paste, contains real traffic data — never commit
freshness-data/input.txt

# Competitor sweep: generated cache/report, not source
tools/competitor-sweep/sweep-cache.json
tools/competitor-sweep/sweep-report.md
